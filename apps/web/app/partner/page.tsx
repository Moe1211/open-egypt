import PartnerClient from './partner-client';

export const metadata = {
  title: 'Partner Portal | Open Egypt',
  robots: {
    index: false,
    follow: false
  }
}

export default function PartnerPage() {
  return <PartnerClient />
}
